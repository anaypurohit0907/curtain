meta {
  name: Delete Row
  type: http
  seq: 5
}

post {
  url: {{base_url}}/db/v1/query
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "query": "DELETE FROM public.products WHERE id = 1"
  }
}
