meta {
  name: Sign Out
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/v1/signout
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
