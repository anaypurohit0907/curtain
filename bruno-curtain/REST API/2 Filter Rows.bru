meta {
  name: Filter Rows
  type: http
  seq: 2
}

get {
  url: {{base_url}}/rest/v1/products?active=eq.true&order=created_at.desc&limit=20
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Accept-Profile: public
}

auth:bearer {
  token: {{token}}
}
